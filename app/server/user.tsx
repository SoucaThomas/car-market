// import { prisma } from "@/prisma/prisma";

// export const getUserFromSessionCookie = (sessionCookie: string) => {
//   const user = prisma.session.findUnique({
//     where: {
//       token: sessionCookie,
//     },
//   });
// };
